1、 langchain 工具调用 和 llm function call 有什么区别？
   langchain对llm function call进行了封装，调用后会直接返回工具的调用情况和调用结果，能够使得开发者更加方便的使用工具。
   llm function call调用后只会返回工具调用情况，需要自己将用户消息和工具描述一起发送给 LLM，在本地执行函数，最后再将执行结果喂回给 LLM 生成最终回复。


2、 langchain 工具调用 的 速度是受到什么影响？
   1.LangChain底层架构；
   2.推理方式；
   3.返回结果解析方式；
   4.上下文编排能力、上下文大小；
   5.工具执行本身的耗时；
   6.缓存策略。

   工具选择 、 工具执行 、 大模型调用
